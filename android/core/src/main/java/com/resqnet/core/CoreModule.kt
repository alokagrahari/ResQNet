package com.resqnet.core

/**
 * Shared Android types, constants, and helpers that are used by more than one
 * feature module. Packet, SOS/location models, and mesh engine code stay in
 * their owning modules (`:mesh`, `:location`).
 */
object CoreModule
